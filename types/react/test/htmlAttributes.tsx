import * as React from 'react';

const htmlSelect = <select
  name="foo"
  multiple={false}
  required={false}
  disabled={true}
  readOnly={true}
  onBlur={this.props.onBlur}
  value={this.props.value}
 />;
